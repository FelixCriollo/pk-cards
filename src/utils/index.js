// retorna un arreglo con el nombre del color y el color en hexadecimal
// const findColorByNameObj = (color, colors) => {
//   const entry = Object.entries(colors).filter(entry => entry[0] === color) 
//   return entry[0]
// } 

// export { findColorByNameObj }